name: sbc-turingrk1
variant: scratch
dependencies:
  - stage: turingrk1
  - stage: kernel
    platform: linux/arm64
  - stage: u-boot
    platform: linux/arm64
  - stage: profiles
finalize:
  - from: /rootfs
    to: /
